- assert:
    that: disk_mount_state in ('present', 'absent')
    quiet: yes

- include_tasks: present.yml
  when: disk_mount_state == 'present'

- include_tasks: absent.yml
  when: disk_mount_state == 'absent'
