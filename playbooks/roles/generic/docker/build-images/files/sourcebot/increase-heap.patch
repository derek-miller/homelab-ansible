--- a/Dockerfile
+++ b/Dockerfile
@@ -92,6 +92,9 @@ COPY --from=shared-libs-builder /app/packages/queryLanguage ./packages/queryLang
 RUN yarn workspace @sourcebot/web install

 ENV NEXT_TELEMETRY_DISABLED=1
+ENV NODE_OPTIONS=--max-old-space-size=4096
+RUN echo '{}' > /tmp/build-config.json
+ENV CONFIG_PATH=/tmp/build-config.json
 RUN yarn workspace @sourcebot/web build
 ENV SKIP_ENV_VALIDATION=0
 # ------------------------------