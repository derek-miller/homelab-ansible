---
- assert:
    that: proxmox_guest_state in ('present', 'absent')
    quiet: yes

- include_tasks: present.yml
  when: proxmox_guest_state == 'present'

- include_tasks: absent.yml
  when: proxmox_guest_state == 'absent'
