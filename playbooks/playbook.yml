- name: set up connection before gathering facts
  hosts: all
  gather_facts: no
  roles:
    - ansible/setup/connection
  tags:
    - always

- name: connect and gather facts
  hosts: all
  tasks:
    - name: node summary
      debug:
        msg: >-
          {{ ansible_all_ipv4_addresses | join(' ') }},
          {{ ansible_distribution }} {{ ansible_distribution_version }},
          {{ ansible_processor_vcpus }} CPU(s),
          {{ ansible_memtotal_mb }} MB memory
  tags:
    - always
    - connect

- name: bootstrap connection credentials
  hosts: all
  roles:
    - role: ansible/setup/bootstrap
      when: bootstrap|default(false)|bool
  tags:
    - bootstrap

- name: bootstrap tailscale
  hosts: tailscale
  roles:
    - role: galaxy/tailscale
      tailscale_authkey: "{{ vault_tailscale_authkey }}"
      when: bootstrap|default(false)|bool
  tags:
    - bootstrap

- name: set up ansible environment on remote
  hosts: all
  roles:
    - ansible/remote
  tags:
    - always
    - base-setup  # not part of base tag since really a setup task but needs apt mirrors, etc. from previous base plays
    - ansible-remote

- name: common
  hosts: all:!qnap
  roles:
    - common
  tags:
    - always
    - base
    - common

- hosts: raspberry-pi
  roles:
    - role: generic/raspberry-pi
  tags:
    - raspberry-pi

- hosts: uctronics-lcd
  roles:
    - role: generic/uctronics-lcd
  tags:
    - uctronics-lcd

- hosts: tailscale
  roles:
    - role: galaxy/tailscale
      tailscale_up_skip: yes # Only authenticate tailscale during bootstrapping
  tasks:
    - name: get tailscale ip addresses
      command: tailscale ip
      changed_when: false
      register: tailscale_ip_result
    - name: set tailscale ip address vars
      set_fact:
        tailscale_ip4_address: "{{ tailscale_ip_result.stdout_lines[0] }}"
        tailscale_ip6_address: "{{ tailscale_ip_result.stdout_lines[1] }}"
    - name: add Cloudflare DNS record for tailscale ipv4 address
      community.general.cloudflare_dns:
        zone: "{{ vault_cloudflare_zone }}"
        account_email: "{{ vault_cloudflare_email }}"
        account_api_token: "{{ vault_cloudflare_api_key }}"
        type: "A"
        record: "{{ inventory_hostname }}.ts"
        value: "{{ tailscale_ip4_address }}"
        ttl: 1
        proxied: no
        solo: yes
      when: create_cloudflare_ipv4_dns_record|default(false)
    - name: add Cloudflare DNS record for tailscale ipv6 address
      community.general.cloudflare_dns:
        zone: "{{ vault_cloudflare_zone }}"
        account_email: "{{ vault_cloudflare_email }}"
        account_api_token: "{{ vault_cloudflare_api_key }}"
        type: "AAAA"
        record: "{{ inventory_hostname }}.ts"
        value: "{{ tailscale_ip6_address }}"
        ttl: 1
        proxied: no
        solo: yes
      when: create_cloudflare_ipv6_dns_record|default(false)
  tags:
    - tailscale

- hosts: nas1-cifs-shares
  roles:
    - role: generic/systemd-cifs-mount
      cifs_mount_state: "{{ nas1_cifs_mount_state | default('present') }}"
      cifs_share_host: "{{ nas1_cifs_share_host }}"
      cifs_share_username: "{{ nas1_cifs_share_username }}"
      cifs_share_password: "{{ nas1_cifs_share_password }}"
      cifs_credentials_path: "/etc/.nas1_cifs_creds"
      cifs_mount_dir: /mnt
      cifs_mount_options: vers=3.0,uid=1000,gid=0,forceuid,forcegid,rw,nofail
      cifs_share_names: "{{ nas1_cifs_share_names | default([]) }}"
  tags:
    - cifs
    - nas1-cifs-shares

- hosts: project-files
  roles:
    - role: generic/project-files
  tags:
    - project-files

- hosts: ddclient
  roles:
    - role: generic/ddclient
  tags:
    - ddclient

- hosts: openvpn-client
  roles:
    - role: generic/openvpn-client
  tags:
    - openvpn-client

- hosts: docker
  roles:
    - role: galaxy/docker
      # Necessary so telegraf can read from the socket
      docker_users: "{{ additional_docker_users|default([]) + ['telegraf'] }}"
      tags:
        - docker-daemon
    - role: generic/docker-stats
      docker_stats_state: "{{ 'present' if docker_service_enabled else 'absent' }}"
      tags:
        - docker-stats
  tags:
    - docker

- hosts: docker_swarm_manager:docker_swarm_worker
  roles:
    - role: galaxy/docker-swarm
  tags:
    - docker
    - docker-swarm

- hosts: docker_swarm_manager[0]
  tasks:
    - name: deploy docker stack
      when: docker_stack_definition is defined
      docker_stack:
        state: "{{ docker_stack_state|default('present') }}"
        name: "{{ docker_stack_name|default(inventory_hostname) }}"
        prune: yes
        compose:
          - "{{ docker_stack_definition }}"
      tags:
        - docker-stack

  tags:
    - docker
    - docker-swarm

- hosts: docker:docker_compose
  tasks:
    - name: create docker containers
      when: docker_compose_definition is defined
      community.general.docker_compose:
        state: "{{ docker_compose_state|default('present') }}"
        remove_images: all
        remove_volumes: yes
        remove_orphans: yes
        project_name: "{{ docker_compose_project_name|default(inventory_hostname) }}"
        definition: "{{ docker_compose_definition }}"
        pull: yes
      tags:
        - docker-compose
  tags:
    - docker

- hosts: msi-rgb
  roles:
    - role: generic/msi-rgb
  tags:
    - msi-rgb

- hosts: x11vnc
  roles:
    - role: generic/x11vnc
  tags:
    - x11vnc

- hosts: network-ups-tools
  roles:
    - role: generic/network-ups-tools
    - role: generic/network-ups-tools-stats
      tags:
        - network-ups-tools-stats
  tags:
    - network-ups-tools

- hosts: tplink-plug-stats
  roles:
    - role: generic/tplink-plug-stats
  tags:
    - tplink-plug-stats

- hosts: coin-stats
  roles:
    - role: generic/coin-stats
      coin_stats_coingecko_ids: "{{ coingecko_ids.split(',') }}"
  tags:
    - coin-stats

- hosts: nicehash
  roles:
    - role: mining/nicehash-stats
      tags:
        - nicehash-stats
  tags:
    - nicehash

- hosts: nvidia-miner
  roles:
    - role: galaxy/nvidia-drivers
      nvidia_driver_ubuntu_cuda_package: cuda
      nvidia_driver_package_version: 11.7.0-1
      nvidia_driver_ubuntu_install_from_cuda_repo: yes
      tags:
        - cuda
        - nvidia-drivers
    - role: mining/nvidia-smi-stats
      tags:
        - nvidia-smi-stats
    - role: mining/nvidia-overclock
      tags:
        - nvidia-overclock
  tags:
    - nvidia
    - nvidia-miner

- hosts: ethereum
  roles:
    - role: mining/ethminer-coin
      ethminer_coin_state: "{{ ethereum_state|default('present') }}"
      ethminer_coin_name: ethereum
      ethminer_coin_pool: "{{ ethereum_pool }}"
      ethminer_coin_api_port: "{{ ethereum_api_port|default('0') }}"
      ethminer_coin_cuda_block_size: "{{ ethereum_cuda_block_size|default(128)|int }}"
      ethminer_coin_cuda_grid_size: "{{ ethereum_cuda_grid_size|default(8192)|int }}"
      ethminer_coin_cuda_streams: "{{ ethereum_cuda_streams|default(2)|int }}"
      ethminer_coin_cuda_schedule: "{{ ethereum_cuda_schedule|default('auto') }}"
      ethminer_coin_overclock_at_start: "{{ ethereum_overclock_at_start|default('no')|bool }}"
      tags:
        - ethminer
  tags:
    - ethereum

- hosts: sysdweb
  roles:
    - role: generic/sysdweb
  tags:
    - sysdweb
